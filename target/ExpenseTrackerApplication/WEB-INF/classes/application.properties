# ========================
# Common Configuration
# ========================
spring.application.name=${SPRING_APPLICATION_NAME:expense-tracker-backend}

spring.security.jwt.secret=${SPRING_SECURITY_JWT_SECRET:mySecretKey123456789012345678901234567890123456789012345678901234567890}
spring.security.jwt.expiration=${SPRING_SECURITY_JWT_EXPIRATION:86400000}

# Server Configuration - use PORT env variable for Render
server.port=${PORT:8086}
server.servlet.context-path=${SERVER_SERVLET_CONTEXT_PATH:/expense-tracker-api}
server.error.whitelabel.enabled=true

# Logging
logging.level.com.expensetracker=${LOGGING_LEVEL_COM_EXPENSETRACKER:DEBUG}
logging.level.org.springframework.security=${LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY:DEBUG}

# Activate profile (h2 for local testing, mysql for production)
# Change to 'h2' for quick local testing without MySQL
spring.profiles.active=${SPRING_PROFILES_ACTIVE:h2}

# Email Configuration (Gmail)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:experiencetracker52@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:dpzm ryqj cisc vwdm}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_REQUIRED:true}
app.email.from=${APP_EMAIL_FROM:siveshkommalapati167@gmail.com}
